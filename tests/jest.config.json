{
  "preset": "ts-jest",
  "globalSetup": "./globalSetup.ts",
  "globalTeardown": "./globalTeardown.ts",
  "setupFilesAfterEnv": ["./setupFile.ts"],
  "transform": {
    "^.+\\.(ts|tsx)?$": "ts-jest",
    "^.+\\.(js|jsx)$": "babel-jest"
  }
}
